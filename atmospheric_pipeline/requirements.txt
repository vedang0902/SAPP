# Atmospheric Monitoring Pipeline — core dependencies
# ------------------------------------------------------------
# TensorFlow (LSTM) is not available for Python 3.13+ on many platforms.
# Use Python 3.10–3.12 and also install:  pip install -r requirements-lstm.txt
#
# Example (recommended local setup):
#   python3.12 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt -r requirements-lstm.txt

pandas>=1.5.0
numpy>=1.21.0
scikit-learn>=1.2.0
scipy>=1.9.0
PyYAML>=6.0
flask>=2.2,<2.3
Werkzeug<2.3
statsmodels>=0.14.0
prometheus_client>=0.17.0
requests>=2.28.0
joblib>=1.3.0
